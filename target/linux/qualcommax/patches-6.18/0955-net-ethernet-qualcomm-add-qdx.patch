Subject: [PATCH] net: qualcomm: build QDX and keep native callers reachable

Build the unified NSS execution driver. Native EDMA and 802.11ax PPE may
operate without QDX; when QDX is modular, cap these callers at module
linkage so their direct references remain reachable.

Signed-off-by: Codex

--- a/drivers/net/ethernet/qualcomm/Kconfig
+++ b/drivers/net/ethernet/qualcomm/Kconfig
@@ -77,16 +77,19 @@
 
 config QCOM_80211AX_PPE
 	tristate "Qualcomm 802.11ax PPE switch driver"
+	depends on QDX || !QDX
 	select PHYLINK
 	help
 	  Driver for Qualcomm 802.11ax PPE (Packet Processing Engine) switches.
 
 config QCOM_EDMA
 	tristate "EDMA dataplane ethernet driver for Qualcomm IPQ platforms"
+	depends on QDX || !QDX
 	help
 	  EDMA dataplane ethernet driver for Qualcomm IPQ platforms.
 
 
+source "drivers/net/ethernet/qualcomm/qdx/Kconfig"
 source "drivers/net/ethernet/qualcomm/rmnet/Kconfig"
 
 endif # NET_VENDOR_QUALCOMM
--- a/drivers/net/ethernet/qualcomm/Makefile
+++ b/drivers/net/ethernet/qualcomm/Makefile
@@ -13,6 +13,7 @@
 obj-$(CONFIG_QCOM_EDMA) += qca_edma.o
 
 obj-y += emac/
+obj-$(CONFIG_QDX) += qdx/
 
 obj-$(CONFIG_QCOM_PPE) += ppe/
 obj-$(CONFIG_RMNET) += rmnet/
