From 492b196e3c0b4e87513b82966221c4365d184d48 Mon Sep 17 00:00:00 2001
From: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
Date: Thu, 28 Nov 2024 13:19:38 +0800
Subject: [PATCH] net: phy: sfp: support shared mod-def0 gpio

The passive SFP/RJ45 mux and the SFP cage both consume MOD_DEF0. Request
that input non-exclusively and treat input flags as a bit mask so the
additional NONEXCLUSIVE flag does not suppress state polling or IRQ setup.

Original MediaTek revision: 492b196e3c0b4e87513b82966221c4365d184d48.
Original subject: net: phy: sfp: support shared mod-def0 gpio.
Adapted to Linux 6.18.

Signed-off-by: Bo-Cun Chen <bc-bocun.chen@mediatek.com>
Signed-off-by: Codex <codex@openai.com>
---
--- a/drivers/net/phy/sfp.c
+++ b/drivers/net/phy/sfp.c
@@ -155,7 +155,7 @@
 };
 
 static const enum gpiod_flags gpio_flags[] = {
-	GPIOD_IN,
+	GPIOD_IN | GPIOD_FLAGS_BIT_NONEXCLUSIVE,
 	GPIOD_IN,
 	GPIOD_IN,
 	GPIOD_ASIS,
@@ -659,7 +659,7 @@
 	unsigned int i, state, v;
 
 	for (i = state = 0; i < GPIO_MAX; i++) {
-		if (gpio_flags[i] != GPIOD_IN || !sfp->gpio[i])
+		if ((gpio_flags[i] & GPIOD_IN) != GPIOD_IN || !sfp->gpio[i])
 			continue;
 
 		v = gpiod_get_value_cansleep(sfp->gpio[i]);
@@ -3275,7 +3275,7 @@
 	}
 
 	for (i = 0; i < GPIO_MAX; i++) {
-		if (gpio_flags[i] != GPIOD_IN || !sfp->gpio[i])
+		if ((gpio_flags[i] & GPIOD_IN) != GPIOD_IN || !sfp->gpio[i])
 			continue;
 
 		sfp->gpio_irq[i] = gpiod_to_irq(sfp->gpio[i]);
